version: 0.2

phases:
  build:
    commands:
      - aws deploy push --application-name kadai-codedeploy --s3-location s3://kadai-s3bucket/artifact.zip --source src
artifacts:
  files:
    - '**/*'
  base-directory: src